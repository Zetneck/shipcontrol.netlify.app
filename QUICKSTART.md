# 🚀 Guía rápida de inicio

## Paso 1: Abrir terminal en la carpeta del proyecto

```bash
cd zaro-instrucciones
```

## Paso 2: Instalar dependencias

```bash
npm install
```

Este comando descargará todas las librerías necesarias (React, Vite, Tailwind, etc.)
El proceso puede tardar 1-3 minutos.

## Paso 3: Ejecutar el proyecto

```bash
npm run dev
```

Verás algo como:
```
VITE v5.0.0  ready in 456 ms

➜  Local:   http://localhost:5173/
➜  press h + enter to show help
```

## Paso 4: Abrir en el navegador

Automáticamente se abrirá en `http://localhost:5173/`

## ✅ Listo para usar

- Completa el campo "Creado por"
- Llena los datos del primer embarque
- Cambia el formato con el toggle (WhatsApp/Hoja Vertical)
- Ve la vista previa a la derecha en tiempo real
- Exporta a PNG o JPG

## 🎨 Personalizar el logo

Reemplaza `/public/zaro-logo.png` con tu logo corporativo en PNG o SVG

## ⚙️ Comandos útiles

| Comando | Descripción |
|---------|-------------|
| `npm run dev` | Ejecutar en desarrollo |
| `npm run build` | Compilar para producción |
| `npm run preview` | Vista previa de build |

## 🐛 Solución de problemas

### "npm: command not found"
→ Instala Node.js desde https://nodejs.org/

### "Port 5173 already in use"
→ Cambia el puerto en `vite.config.js` o cierra la otra aplicación

### El logo no se ve
→ Asegúrate que `/public/zaro-logo.png` existe y tiene formato válido

---

¡Disfruta de Zaro Instrucciones! 🎉
